node_modules/
dist/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environment files hold database URIs, JWT secrets and SMTP passwords and must
# never be committed. The `.example` templates and the placeholder-only
# `.env.production` are tracked on purpose.
.env
.env.local
.env.*.local
!.env.example
!.env.production

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
